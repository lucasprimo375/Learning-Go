package auxiliar

import "fmt"

func secondAuxiliarWrite() {
	// private functions in the packaged must have name starting with lower case character

	fmt.Println("Writing from second write function in auxiliar package")
}